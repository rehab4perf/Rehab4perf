[[headers]]
  for = "/*"
  [headers.values]
    # Clickjacking protection
    X-Frame-Options = "SAMEORIGIN"
    # MIME-sniffing protection
    X-Content-Type-Options = "nosniff"
    # Referrer privacy
    Referrer-Policy = "strict-origin-when-cross-origin"
    # Force HTTPS (1 an, sous-domaines inclus)
    Strict-Transport-Security = "max-age=31536000; includeSubDomains"
    # Content Security Policy
    # - script-src  : app inline + SDK Supabase via jsdelivr
    # - style-src   : styles inline (app ne charge aucun CSS externe)
    # - connect-src : REST + WebSocket Supabase (wss: pour realtime SDK)
    # - img-src     : data:/blob: graphiques + thumbnails YouTube
    # - frame-src   : embeds YouTube (player vidéo exercices)
    # - font-src    : polices locales uniquement
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline'; connect-src 'self' https://sxdobjodxkwexaspepdm.supabase.co wss://sxdobjodxkwexaspepdm.supabase.co; img-src 'self' data: blob: https://img.youtube.com; frame-src https://www.youtube.com; font-src 'self'; object-src 'none'; base-uri 'self';"
